"use client"
export default function ModeracaoAutomodRegrasPage() { return (<div className="space-y-4"><h1 className="text-xl font-bold">AutoMod — Regras</h1>
  <div className="space-y-2">{[{ name: "Anti-spam", enabled: true, desc: "Detecta mensagens repetitivas" }, { name: "Anti-phishing", enabled: true, desc: "URLs suspeitas" }, { name: "Linguagem tóxica", enabled: true, desc: "ML-based detection" }, { name: "Self-promo", enabled: false, desc: "Links para outros canais" }].map(r => <div key={r.name} className="p-3 rounded-xl border border-white/10 flex items-center justify-between"><div><p className="text-xs font-semibold">{r.name}</p><p className="text-[10px] text-muted-foreground">{r.desc}</p></div><span className={`text-[10px] ${r.enabled ? "text-green-400" : "text-red-400"}`}>{r.enabled ? "Activo" : "Inactivo"}</span></div>)}</div></div>) }
